# .pre-commit-config.yaml
repos:
- repo: local
  hooks:
    - id: executable-bit-checker
      name: Check hooks have executable bit set
      entry: ./checker.sh # or python your_script.py
      language: script # or python, bash, system, etc.

